# Install

```
chsh -s $(which zsh)

sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"

chsh -s $(which bash)
```

# Usage

```
chsh -s $(which zsh)

# open a new terminal
# cd to some git repo working directory
# then you should see the branch name
```